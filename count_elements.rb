def count_elements(array)
 new_hash = {}
 count = 0
    array.each do |key|
      if key == new_hash.keys
        count += 1
      @new_hash[keyto_s] = count
    end
  end
end
  # @play_area[x] ||= []